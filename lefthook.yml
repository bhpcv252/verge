pre-commit:
  commands:
    checks:
      run: |
        make lint &&
        make test &&
        go fmt ./...

    proto-check:
      run: make proto-check

pre-push:
  commands:
    integration:
      run: make test-all
